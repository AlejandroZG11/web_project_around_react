// src/contexts/CurrentUserContext.js

import { createContext } from 'react';

const CurrentUserContext = createContext(null); // Inicializado a null para evitar posibles problemas

export default CurrentUserContext;